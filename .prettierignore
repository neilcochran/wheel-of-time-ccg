node_modules/
dist/
coverage/
*.tsbuildinfo

# Written byte for byte by the card importer and compared against on
# `pnpm import-cards:check`. Reformatting it would break that comparison.
packages/cards/generated/

# Source data and scans, not source code.
data/

# Generated by pnpm. Reformatting it produces a huge diff on every install.
pnpm-lock.yaml
